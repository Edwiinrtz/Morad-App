package com.edwiinrtz.morad_app.model.services

import com.google.android.datatransport.runtime.dagger.MapKey
import com.google.firebase.messaging.FirebaseMessagingService
import com.google.firebase.messaging.RemoteMessage
import com.google.firebase.messaging.RemoteMessage.Notification
import java.time.LocalTime

class MyFirebaseMessagingService: FirebaseMessagingService(){



    /*fun createNotification(){

        var nn= mapOf("noti" to "noti")
        //val noti = RemoteMessage.Builder().set

        /*val noti = Notification.builder().setBody("nueva nota creada").setTitle("CORRE!").build()*/
        //val message = Message.builder().setTopic("3003").setNotification(noti).putData("time", LocalTime.now().toString()+120).build()

        //FirebaseMessaging.getInstance().send(message)

    }*/

}